name: Desktop Release

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Version tag (e.g. v1.0.0)'
        required: true

permissions:
  contents: write

jobs:
  build-mac-arm64:
    name: Build macOS (Apple Silicon)
    runs-on: macos-14
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10.8.1
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Build
        run: pnpm run build
      - name: Package (macOS arm64)
        run: pnpm exec electron-builder --mac --arm64
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # NOTE: Mac Intel (x64) is intentionally NOT a separate job.
  # `mac.target` in package.json lists both arm64 and x64, and the arm64 runner
  # cross-builds the x64 DMG in the same `electron-builder --mac --arm64` step.
  # On v0.1.3 the dedicated macos-13 Intel job stalled in the runner queue for
  # 30+ min while the arm64 job had already published both arches. Keeping a
  # standalone Intel job just blocks publish-aliases for no benefit.
  # If pure-x64 builds are ever needed (e.g. signing differences), reintroduce
  # the job and split mac.target between the two configs.

  build-win:
    name: Build Windows
    runs-on: windows-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10.8.1
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Build
        run: pnpm run build
      - name: Package (Windows)
        run: pnpm exec electron-builder --win --x64
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  publish-aliases:
    name: Publish stable-name aliases
    needs: [build-mac-arm64, build-win]
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Determine tag
        id: tag
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "tag=${{ github.event.inputs.tag }}" >> $GITHUB_OUTPUT
          else
            echo "tag=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
          fi
      - name: Download and re-upload with stable names
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ steps.tag.outputs.tag }}
        run: |
          gh release download "$TAG" --pattern "*.dmg" --pattern "*.exe" --dir ./artifacts
          ls artifacts/
          # Rename to stable names
          for f in artifacts/*arm64*.dmg; do cp "$f" artifacts/Nomi-mac-arm64.dmg 2>/dev/null || true; done
          for f in artifacts/*x64*.dmg; do cp "$f" artifacts/Nomi-mac-intel.dmg 2>/dev/null || true; done
          for f in artifacts/*.exe; do cp "$f" artifacts/Nomi-windows-setup.exe 2>/dev/null || true; done
          # Upload stable-name aliases
          gh release upload "$TAG" artifacts/Nomi-mac-arm64.dmg --clobber 2>/dev/null || true
          gh release upload "$TAG" artifacts/Nomi-mac-intel.dmg --clobber 2>/dev/null || true
          gh release upload "$TAG" artifacts/Nomi-windows-setup.exe --clobber 2>/dev/null || true
